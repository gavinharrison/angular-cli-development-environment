#!/bin/bash

# TODO : Add checks to make sure that a package.json file exists before running the below commands.
# other wise you get an error from npm install saying that it could not find the package.json file.

npm install
npm start